import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ChooseRole = () => {
  return (
    <View>
      <Text>ChooseRole</Text>
    </View>
  );
};

export default ChooseRole;

const styles = StyleSheet.create({});
